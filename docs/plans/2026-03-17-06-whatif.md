# Plan 06 — "Et si..." Simulator

> **For agentic workers:** REQUIRED: Use `superpowers:executing-plans` to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the "Et si..." free-form scenario editor — pre-filled with current simulation values, any field editable, results recalculate in real-time, split-screen before/after view with highlighted deltas.

**Architecture:** `WhatIfContext` wraps a local copy of `SimulationInput` initialized from the main `SimulationContext`. Edits are local — they never mutate the "real" simulation. `calculate()` runs on every state change (debounced). Split screen shows frozen "before" snapshot on the left, live "after" on the right. "Copy link" encodes the modified state into a new URL.

**Tech Stack:** React, TypeScript, Tailwind. `useDeferredValue` or debounce for real-time calc.

**Prerequisite:** Plans 01 + 02 + 03 + 04 complete.

> **Note (from Plan 04):** The "Et si..." tab in `/simulate/page.tsx` currently renders a "coming soon" placeholder. Plan 06 should either (a) replace that placeholder with `<WhatIfShell />` directly inside the tab panel (consistent with how ResultsShell works), or (b) remove the tab and keep `/simulate/whatif` as a standalone route accessed only via the CTA. The existing CTA in `ResultsShell` already points to `/simulate/whatif`.
>
> **Note (from Plan 03):** M4 and M5 return `M4Result` / `M5Result` (not plain `ModelResult`). `WhatIfContext` should use `CalculationResults` from `calculate()` directly rather than trying to access `.modelResult` or `.optionB` without the type guards already in `ComparisonTable`.

---

## File Map

```
src/
  context/
    WhatIfContext.tsx            ← local copy of SimulationInput, isolated from main context
  components/
    whatif/
      WhatIfShell.tsx            ← split-screen layout (before | arrow | after)
      WhatIfPanel.tsx            ← editable form panel (after scenario)
      SnapshotPanel.tsx          ← frozen read-only panel (before)
      DeltaRow.tsx               ← value row with diff indicator (↑/↓ + color + amount)
      WhatIfSummary.tsx          ← narrative summary of impact ("L'écart passe de X à Y")
  app/
    simulate/
      whatif/
        page.tsx                 ← /simulate/whatif route

tests/unit/components/whatif/
  WhatIfContext.test.tsx
  DeltaRow.test.tsx
  WhatIfSummary.test.tsx
tests/e2e/
  whatif.spec.ts
```

---

## Task 1: WhatIfContext

**Files:** `src/context/WhatIfContext.tsx`, `tests/unit/components/whatif/WhatIfContext.test.tsx`

- [x] **Step 1: Write failing tests**

  ```typescript
  // Test: initializes from SimulationContext snapshot (deep copy, not reference)
  // Test: modifying WhatIf state does NOT mutate SimulationContext
  // Test: reset() restores initial snapshot
  // Test: isDirty = true when any field differs from snapshot
  // Test: results are recalculated when input changes
  ```

- [x] **Step 2: Implement** — deep clone on init, local useReducer, `calculate()` on every change
- [x] **Step 3: Run tests**
- [x] **Step 4: Commit** `feat(whatif): WhatIfContext isolated from main simulation`

---

## Task 2: DeltaRow component

**Files:** `src/components/whatif/DeltaRow.tsx`, `tests/unit/components/whatif/DeltaRow.test.tsx`

- [x] **Step 1: Write failing tests**

  ```typescript
  // Test: shows label, before value, after value
  // Test: no delta indicator when values are equal
  // Test: green ↓ arrow + green text when after < before (for gap/contribution)
  // Test: red ↑ arrow when after > before
  // Test: shows diff amount "+ 124 €" or "- 263 €"
  // Test: "changed" prop highlights the row background
  ```

- [x] **Step 2: Implement**
- [x] **Step 3: Run tests**
- [x] **Step 4: Commit** `feat(whatif): DeltaRow with directional diff indicator`

---

## Task 3: WhatIfSummary

**Files:** `src/components/whatif/WhatIfSummary.tsx`, `tests/unit/components/whatif/WhatIfSummary.test.tsx`

- [x] **Step 1: Write failing tests**

  ```typescript
  // Test: shows green box when equity gap decreased
  // Test: shows amber box when equity gap increased
  // Test: narrative uses real names ("L'écart de Thomas et Léa passe de X à Y")
  // Test: shows annual impact ("soit X € d'écart en moins par an")
  // Test: shows nothing (or neutral message) when nothing has changed
  ```

- [x] **Step 2: Implement**
- [x] **Step 3: Run tests**
- [x] **Step 4: Commit** `feat(whatif): WhatIfSummary narrative impact block`

---

## Task 4: WhatIfPanel (editable form)

**Files:** `src/components/whatif/WhatIfPanel.tsx`

- [x] **Step 1: Write failing tests**

  ```typescript
  // Test: all fields pre-filled with current simulation values
  // Test: editing income P1 triggers recalculation (result changes)
  // Test: editing a slider updates domestic values
  // Test: "Reset" button restores all fields to initial snapshot values
  // Test: "Copier le lien de ce scénario" generates URL with modified state
  ```

- [x] **Step 2: Implement** — use same form components as main form (FormField, SelectField, SliderField)
- [x] **Step 3: Run tests**
- [x] **Step 4: Commit** `feat(whatif): WhatIfPanel editable form with reset`

---

## Task 5: Split-screen layout + /whatif route

**Files:** `src/components/whatif/WhatIfShell.tsx`, `src/app/simulate/whatif/page.tsx`

- [x] **Step 1: Write failing tests (unit)**

  ```typescript
  // Test: SnapshotPanel shows before values (read-only)
  // Test: WhatIfPanel shows after values (editable)
  // Test: arrow icon between panels
  // Test: mobile: stacked layout (before on top, after below)
  ```

- [ ] **Step 2: E2E test** `tests/e2e/whatif.spec.ts` — deferred to Plan 07

- [x] **Step 3: Implement** — responsive split-screen
- [x] **Step 4: Run tests**
- [x] **Step 5: Commit** `feat(whatif): split-screen Et si... layout with real-time recalculation`

---

## Definition of Done

- [x] All fields in current simulation are editable in "Et si..."
- [x] Results recalculate in real-time on any change (debounced < 100ms)
- [x] Deltas are visually clear (color, arrow, amount)
- [x] Reset restores original values
- [x] Copy link encodes modified scenario
- [x] Mobile layout stacks panels vertically
- [ ] E2E tests passing — deferred to Plan 07

---

## Plan Status

| Date       | Status      | Notes                                                                    |
| ---------- | ----------- | ------------------------------------------------------------------------ |
| 2026-03-17 | 📋 Draft    | Consider using `useDeferredValue` (React 18) rather than manual debounce |
| 2026-03-18 | ✅ Complete | All unit tests pass (32 tests). E2E deferred to Plan 07.                 |

---

## Divergences from Spec / Plan

| Decision | Plan says | Actual | Reason |
| -------- | --------- | ------ | ------ |
| —        | —         | —      | —      |
